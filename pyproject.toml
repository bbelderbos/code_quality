[project]
name = "code-quality"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "radon>=6.0.1",
    "complexipy>=5.0.0",
    "python-decouple>=3.8",
    "textual>=6.6.0",
]
